<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Remember-Me: <%- title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/highlightjs/cdn-release@11.7.0/build/styles/github.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
    <style>
        body {
            font-size: 1.1rem;
            background: #f8f8f8;
        }
        .avatar {
            min-width: 320px;
            height: 320px;
            border-radius: 50%;
            background-color: #E5D30F;
            overflow: hidden;
        }
        .hljs {
            background: #ebebeb;
        }
        pre code.hljs {
            padding: 1.4rem;
            border-radius: 1rem;
        }
        .doc h1 { margin-bottom: 2.2rem; margin-top: 1rem; font-weight: bolder; }
        .doc h1:first-child { margin-top: 0; }
        .doc h2 { margin-bottom: 1.8rem; margin-top: 1rem; font-weight: bolder; border-top: 1px solid #e7e7e7; padding-top: 1.2rem; color: var(--bs-info); }
        .doc h3 { margin-bottom: 1.6rem; margin-top: 1rem; opacity: .8; }
        .doc h4 { margin-bottom: 1.2rem; margin-top: 1rem; opacity: .8; }

        main,
        .navbar {
            margin: auto;
        }

        @media (min-width: 1080px) {
            main,
            .navbar {
                max-width: 1080px !important;
            }
        }

        @media (min-width: 768px) {
            .navbar-collapse {
                border: none !important;
            }
        }

        main {
            padding: 1.4rem;
            background: #f6f3f3;
        }
    </style>
</head>
<body style="background: #e3e3e3;">
    <header class="bg-info">
        <nav class="navbar navbar-expand-md navbar-dark">
            <div class="container">
                <a class="navbar-brand py-2 lh-1 me-5" href="/">
                    <small class="d-block">Passport</small>
                    <span class="fw-bolder fs-3">Remember Me Strategy</span>
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse border-top border-light border-opacity-25 mt-2 mt-md-0" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link fw-bold px-md-4 <% if (page === "/") { %>active<% } %>" href="/">Docs</a>
                        </li>
                        <% if (!user) { %>
                            <li class="nav-item">
                                <a class="nav-link fw-bold px-md-4 <% if (page === "/sign-in") { %>active<% } %>" href="/sign-in">Sign-in</a>
                            </li>
                        <% } else { %>
                            <li class="nav-item">
                                <a class="nav-link fw-bold px-md-4 <% if (page === "/account") { %>active<% } %>" href="/account">Account</a>
                            </li>
                        <% } %>
                    </ul>
                    <div class="flex-fill"></div>
                    <% if (user) { %>
                        <a class="btn btn-light d-block d-md-inline me-md-4" href="/sign-out">Sign-Out</a>
                    <% } %>
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link fw-bold px-md-4 px-md-0" href="https://github.com/jairmilanes/passport-remember-me">
                                <i class="bi bi-github fs-3 d-none d-md-inline-block"></i> <span class="d-md-none fw-bold">Github</span>
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <main class="container my-5 w-auto mx-3 w-md-100 mx-sm-auto">
        <%- body %>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js" integrity="sha384-mQ93GR66B00ZXjt0YO5KlohRA5SY2XofN4zfuZxLkoj1gXtW8ANNCe9d5Y3eG5eD" crossorigin="anonymous"></script>
</body>
</html>